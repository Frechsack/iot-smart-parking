[Unit]
Description=Overwatch autostart
After=docker.service

[Service]
Type=simple
User=root
ExecStart=docker run --device /dev/video0 iot-smart-parking-overwatch -d --rm  --name iot-smart-parking-overwatch

[Install]
WantedBy=multi-user.target
